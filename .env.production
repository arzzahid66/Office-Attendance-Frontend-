# Full backend API base URL used for production builds (Vercel runs `vite build`).
# NOTE the "/api" suffix — the app calls paths like "/auth/login" on top of this.
# If your backend URL ever changes, change it HERE (one place) and redeploy.
VITE_API_BASE_URL=https://attendance-be.xeventechnologies.com/api
