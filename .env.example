# Copy to .env.production (or set in the Vercel dashboard) and fill in your backend URL.
# Must include the "/api" suffix. Leave unset in local dev — the Vite proxy handles /api.
VITE_API_BASE_URL=https://your-backend-domain/api
